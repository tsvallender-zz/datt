<%= provide :title, 'Notes' %>

<% @notes.each do |note| %>
  <div class="note">
    <%= link_to note do %>
      <h4 class="title"><%= note.title %></h4>
    <% end %>
    <div class="content"><%= note.content %></div>
  </div>
<% end %>